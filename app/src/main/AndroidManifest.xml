<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daytoday.business.dailydelivery">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme">
        <activity android:name=".MainHomeScreen.EmpInfoActivity"></activity>
        <activity
            android:name=".MainHomeScreen.HomeScreen"
            android:theme="@style/SplashTheme.NoActionBarNavBar" />
        <activity android:name=".WalkThrough.WalkThroughActivity" />
        <activity android:name=".BlankActivity" />
        <activity android:name=".LoginActivity.WelcomePage" />
        <activity android:name=".LoginActivity.verification_activity" />
        <activity android:name=".LoginActivity.activity_login" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>